我们拿一个真实的 Claude Code session 做了成本透视：7 分钟、一次代码 review 任务、257 行本地日志。把"看起来是多少"和"实际计费是多少"放在一起——**按界面轮数记账的算法，把这次 session 的输入量多算了 3.3 倍，把决策次数多算了 3 倍，并且完全看不见占输入流量 89% 的东西。**

不是哪个工具的 bug。是所有"按日志条数记账"的成本报告共有的结构性盲区。

先把主语说清楚：**官方账单没有错。**供应商按真实的计费语义收钱——全价输入、约 1/10 的缓存读、输出，一次不多收。算错的是**从日志重算成本的那本私账**：把流式响应的重复记录当多次调用、把 `input_tokens` 直接加总、无视缓存字段，重算出来的数就会比真实计费高 3 倍——我们的手工统计就是这个反面的活体样本。

随后我们用 [ccusage](https://github.com/ryoppippi/ccusage) 对同一 session 对账，结果是它**逐 token 正确**：input 103,616、output 6,734、cache read 866,560，与去重后的结果完全一致。成熟工具的计量没有问题。但这恰恰引出本文真正的问题：**计量已解决，诊断仍空白**——缓存占比 89.3%、冷启动占 30%、一条命令吃掉 16%，这些结构性事实，没有任何工具会主动算给你看。

## 透视对象

- 来源：`~/.claude/projects/` 下一个真实 session 的 JSONL（257 行）
- 任务：一次代码 review（某私有仓库；本仓库即其公开复现与脚本）
- 模型：glm-5-3-flash（经 Claude Code 代理）
- 界面观感：33 轮对话、7 分钟

## 发现一：33 轮是假的，真实 API 调用只有 11 次

日志里有 33 条 assistant 记录，但每条记录的 `usage` 字段（输入/输出 token 数）是**按 API 响应重复记账的**——一次流式响应会被拆成 2–4 条记录，每条都带着同一份 usage 快照。真正的去重键是 `message.id`：

| 记账方式 | 次数 |
|---|---|
| 按日志条数（大多数工具的做法） | 33 |
| 按 `message.id` 去重（真实计费次数） | **11** |

如果你看过任何一张"每个决策花了多少钱"的报表，先问一句：它去重了吗？

## 发现二：真实计费输入是 10.4 万，不是 33.9 万

把 33 条记录的 `input_tokens` 直接加总，会得到 339,088——这是重复记账后的假数。去重后的真实值：

| 指标 | naive 加总 | 按 message.id 去重 |
|---|---|---|
| fresh input | 339,088 | **103,616** |
| output | 16,245 | **6,734** |
| API 调用 | 33 | **11** |

## 发现三：输入流量的 89.3% 是缓存命中——它根本不出现在你的账单叙事里

去重之后，这个 session 的真实输入流量是 103,616（fresh）+ 866,560（`cache_read_input_tokens`）= 970,176 tokens。**缓存命中占 89.3%。**

这就是 Agent 成本的真实结构：会话开始时付一次全价（第一次调用 66,609 tokens，cache 为 0），之后每一轮只对增量付全价、对整个历史按约 1/10 的缓存价重读。上下文从 66.6K 涨到 104K（+56%）——"上下文税"存在，但税率是 0.1，不是 1。

## 那钱到底花在哪了

在没有定价表的情况下，用典型比价（fresh input = 1、cache read = 0.1、output = 5）估算账单结构：

- **fresh input ≈ 46%**——其中 **冷启动一次就占全账单约 30%**（第一次调用别无选择的全价 66K）
- **cache 重读 ≈ 39%**——上下文税的真实形态，温和但每轮都在涨
- **output ≈ 15%**——最后那次长回复一条就占了全部 output 的 31%

合起来一句话：**账单的 85% 是记忆操作**——把上下文装进脑子、把历史反复读回去；思考的产物只占 15%。典型比价下，**记忆比思考贵 5.7 倍**。

单点异常也很扎眼：第 5 次调用的一条 Bash 结果注入了 27,189 个全价 token，一条命令吃掉全账单的约 16%。`git diff` 这种大输出命令，就是 Agent 成本里最典型的"隐形大单"。

对使用者的启示也因此不同：**能优化的不是"少聊几轮"，而是"别让工具结果爆量"**（定向 Read 替代 cat/diff 全量输出）和"及时开新会话"（避免把 10 万 token 的历史拖进每个新任务）。这与 百炼账单降低 88% 的六刀优化 是同一结论的日志级证据；而 Agent 整体 token 消耗已是人类 5.2 倍（见 Agent token 用量超人类 5.2 倍），这类浪费是在整个行业的账单上叠加的。

## 自己动手，30 秒

把下面这段跑在你自己的日志上（macOS/Linux，装有 Claude Code 即可）：

```python
import json, glob
calls = {}
for fp in glob.glob('~/.claude/projects/*/*.jsonl'):
    for line in open(fp):
        r = json.loads(line)
        if r.get('type') != 'assistant': continue
        calls[r['message']['id']] = r['message']['usage']  # message.id 去重
fresh = sum(u['input_tokens'] for u in calls.values())
cache = sum(u['cache_read_input_tokens'] for u in calls.values())
out   = sum(u['output_tokens'] for u in calls.values())
print(f"API 调用: {len(calls)} | fresh: {fresh:,} | cache_read: {cache:,} ({cache/(fresh+cache):.0%}) | output: {out:,}")
```

对比一下你现有成本工具给出的"输入 token 总量"——如果两个数对不上，现在你知道差在哪了。

## 对账之后：计量已解决，诊断仍空白

对账改变了本文的矛头。ccusage 的计量逐 token 正确——去重和缓存拆分这两条，成熟工具已经做到。下面三条要求的对象，从此是**自己动手数日志的人**：

1. **按 `message.id` 去重**——日志条数 ≠ 计费次数，流式响应会被记 2–4 条。
2. **拆开缓存**——`input_tokens` 只是全价部分；`cache_read_input_tokens` 是另一个数量级的流量、另一个价签。不拆缓存的"总输入量"没有意义。
3. **定价表按模型走**——日志里没有钱，只有 token；换模型=换价签，别用统一单价估。

而真正的空白在计量之后：**缓存占比 89.3%、冷启动占 30%、一条 Bash 结果吃掉 16%——这些结构性诊断，没有任何工具会主动算给你看。**ccusage 回答"花了多少"精确到个位；"钱的结构、浪费的形态"仍是每个重度用户自己面对的空白页。

## 换一个工具还成立吗

成立。通用的是**三步法**——定位本地日志目录 → 按 call id 去重 → 把 token 拆成 fresh / cache-read / output 三桶——每个工具只是这之上一个几十行的 schema 适配器。因为底下是不变量：Anthropic 系和 OpenAI 系 API 的 usage 语义（全价输入 / 缓存读 / 输出）是各家日志共同包装的对象。ccusage 支持 8 种工具已经证明适配器后勤是可解的。按数据源质量分三档：

| 档位 | 工具 | 日志位置 | 状态 |
|---|---|---|---|
| 一档：字段齐全 | Claude Code | `~/.claude/projects/*/*.jsonl` | ✅ 本文已验证（usage 四件套 + `message.id`） |
| 一档：字段齐全 | ZCode | `~/.zcode/cli/rollout/model-io-sess_*.jsonl` | ✅ 已验证（每次调用完整请求体 + 精确 usage） |
| 一档：字段齐全 | Codex CLI | `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl` | ✅ 已验证：`token_count` 事件含 fresh / cache-read / cache-write 三桶，reasoning 输出单列，还附 5 小时/每周配额百分比 |
| 二档：需导出/字段较少 | OpenCode | `~/.local/share/opencode/storage/`（已核实：无 usage 暴露） | ⚠️ 消息正文与 usage 不在本地存储，标准读法需先 `opencode export` |
| 二档：需导出/字段较少 | pi agent、Hermes 等 | 各自本地目录 | ⚠️ 已核实：Hermes 会话为纯对话 JSONL、无 usage 字段，只能按消息体积降级估算；pi agent 未测 |
| 三档：方法边界 | DeepSeek 网页版等纯云工具 | **无本地日志** | ❌ 零接入方法原理上不可见；除非服务方提供导出，否则只能靠人工记录 |

一句话：**本地有日志的工具都能检到，纯云工具是这条方法的物理边界。**

## 统计口径与误差边界

先分层：**token 数是事实层**（runtime 响应自带的 usage 字段，本文只做去重和加总）；**美元和占比是推算层**（比价假设的函数）。两层可信度不同，引用时别混。

- **去重安全性**：同一 `message.id` 的重复记录 usage 完全一致（本 session 11 个 id、0 个不一致），按 id 去重不引入误差。
- **切分模糊性**：`fresh input` 与 `cache_read` 的边界由系统缓存断点决定，不是内容边界——逐调用守恒校验显示每处有 ±几百 token 的系统级模糊（约占上下文的 0.2–2%）。所以本文只对远超噪声的量级下结论（冷启动 30%、单条 27K 大单），不对单次调用做精确归因。
- **版本锚定**：本口径在 Claude Code v2.1.153 上验证；runtime 更新即可能漂移日志格式，口径需随版本重验。
- **无账单对账**：本次拿不到供应商侧账单做金标准比对（GLM 经代理转发，`cache_creation` 恒 0，疑似代理丢失该字段——这意味着真实账单可能与我们估算再有出入）。有 API 账单的读者可用文末脚本自行对账。
- **n=1**：本文只声称这个 session 的结构成立，不声称行业分布。

## 局限

单一样本（1 个 session、1 个模型、1 种 harness）；`cache_creation` 在这个代理端恒为 0，Anthropic 直连的账单结构会多一个"缓存写入"项；比价为典型假设，换算成美元需要各模型定价表。结论的结构（去重、缓存占比、冷启动占比）在不同 session 间应当稳定，具体百分比会浮动。

---

*数据与复现脚本：CostLens spike（2026-09-05），对象 session `d823d9e5`。*
